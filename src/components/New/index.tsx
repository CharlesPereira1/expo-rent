import React from "react";
import { View } from "react-native";

import styles from "./styles";

interface NewProps {
  cover: string;
  name: string;
  description: string;
}

const New: React.FC = ({ cover, name, description }) => {
  return <View />;
};

export default New;
