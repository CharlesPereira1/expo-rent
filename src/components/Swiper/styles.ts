import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    backgroundColor: "#fff",
  },
  slide: {
    flexDirection: "row",
    height: 340,
    width: "100%",
  },
});
